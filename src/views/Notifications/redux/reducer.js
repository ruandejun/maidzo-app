import actions from './action'

const initState = {
    notifications: [],
    isFetching: false
};

export default function appReducer(state = initState, action) {
    switch (action.type) {
        default:
            return state
  }
}
